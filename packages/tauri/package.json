{
  "name": "@solana-auto-exit/tauri",
  "version": "0.3.1",
  "private": true,
  "description": "Tauri shell que envuelve el web bundle + servidor como sidecar para distribución desktop (Win/Mac/Linux).",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
